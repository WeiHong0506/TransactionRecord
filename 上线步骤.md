# v1.8.0 上线步骤

GitHub 上现在是 **v1.6.1**。这个包把之后积压的四件事一次性做完：

| 内容 | 原本的版本号 |
|---|---|
| 底栏和页面标题改为「资产」 | v1.6.3 |
| 多货币（账户带币种、汇率折算） | v1.7.0 |
| 表单打开时背景仍可滑动的 bug | — |
| 自建金额键盘（算式 + 服务费 / SST） | v1.8.0 |

---

## 第 1 步：先跑 Supabase SQL（必须在推代码之前）

`supabase-migrate-v1.7.0.sql` 整段贴进 Supabase → SQL Editor → Run。

它给 `transactions` 和 `accounts` 各加一个 `currency` 列，默认 `MYR`——这个默认值正好让所有历史行自动获得正确的货币。用了 `if not exists`，重复跑不会报错。

**顺序不能反。** 先推代码的话，应用会往还没有 `currency` 列的表上传数据，同步直接报错。

跑完确认最后那三句验证：两个 `currency` 列都在、每种货币的行数对得上、三张表的 `rls_enabled` 全是 `true`。

## 第 2 步：覆盖文件

把这个包里 `src/`、`scripts/`、`mock/` 三个目录和根目录的几个文件，覆盖到仓库同名位置。**不要删 `上线步骤.md` 之外的任何现有文件**，这个包只包含改动过的。

## 第 3 步：删掉一个孤儿文件

```
src/components/TransactionSheet.jsx
```

v1.6.1 时它已经被 `TransactionPage.jsx` 取代，没人再 import 它。留着不影响构建，但会一直躺在仓库里。

## 第 4 步：验证再推

```
npm install
npm run build
```

想跑测试的话（需要 Playwright 的 Chromium）：

```
npm run preview          # 另开一个终端
node scripts/smoke-test.mjs
node scripts/import-test.mjs
node scripts/sync-logic-test.mjs
node scripts/fx-test.mjs
node scripts/pad-test.mjs
```

云端环境里这五套分别是 17 / 25 / 24 / 28 / 41 项，全绿。后三套不需要浏览器，直接 `node` 就能跑。

```
git add -A
git commit -m "feat: 多货币、自建金额键盘、资产改名、修复表单背景滚动"
git push
```

## 第 5 步：推完之后

1. 硬刷新（手机上把应用从主屏幕关掉重开），确认设置 → 关于显示 **v1.8.0**
2. 本地数据库会自动升到 **v6**，把现有记录和账户全部标成 MYR。金额本身一个字节都不会动，这一步有测试覆盖。

---

## 这一版改了什么

### 资产

底栏第二个 tab 和页面标题都叫「资产」。代码注释、CSS 分区、测试脚本、README 里的「账号管理」一并统一。

### 多货币

- 每个账户带一种货币，新建账户时选；**已有流水的账户不能再改币种**，改了等于把历史金额的含义整体改掉。
- 账户余额永远用它自己的货币算，和你手机里的真实余额一分不差。折算只发生在汇总成总资产那一步。
- 设置 → 汇率手填，不联网。只列出你真的有账户在用的外币。
- **没设汇率时不按 1:1 凑合**——那个货币的账户和记录直接排除在总资产和统计之外，页面顶上给黄色提示。宁可少算并说出来，也不要编一个看起来正常的错数。
- 换主货币时汇率自动重新归一（1 CNY = 0.63 MYR → 1 MYR ≈ 1.587 CNY），不然所有折算数字会一夜之间全错。
- CSV 导出多了「货币」和「折算」两列。

### 表单背景滚动

iOS Safari 上光靠 `body{overflow:hidden}` 锁不住背景。改成把 body 整个 `position:fixed` 钉住并记下滚动位置，关闭时滚回去；`.page-body` 加 `overscroll-behavior: contain` 阻断滚动传递。

### 自建金额键盘

- 页面上**再没有金额输入框**，系统键盘永远不会弹——这本来就是背景滚动那个 bug 的根源。
- 支持 `+ − × ÷`，算式实时显示。没有等号键，「完成」自己会先算再提交。
- 服务费和 SST 开关，**两项都按小计算，互不叠加**。小票式明细逐行拆开，能拿着账单对。
- 税率点一下就能改，用同一套键盘输入，不唤起系统键盘。**税率记住**（是配置），**开关每次归零**（是这一笔的选择）。
- 归零发生在「记一笔」页面打开时，不是键盘每次弹出时——关掉键盘不等于这笔记完了。
- 电脑上物理键盘照常能用：数字、`+ - * /`、退格、回车、Esc。
- **只存最终金额**，不存拆解。所以已保存的记录点开后看不到「原价 + 税率」，金额旁的税费徽记也只在保存前出现。

### 新增文件

| 文件 | 作用 |
|---|---|
| `src/padMath.js` | 键盘算术内核，纯函数，脱离浏览器可测 |
| `src/components/AmountPad.jsx` | 键盘组件 |
| `scripts/fx-test.mjs` | 汇率与 v6 迁移测试（28 项） |
| `scripts/pad-test.mjs` | 键盘算术与税费测试（41 项） |
| `mock/keypad.html` | 键盘的可交互草案，不参与构建 |

`scripts/pad-test.mjs` 里钉死了那条验算：**两个税率填成一样时，两行金额必须完全相等**。以后谁手滑改成叠加，这条会立刻红。
